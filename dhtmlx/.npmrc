# The DHTMLX trial build lives on DHTMLX's own registry rather than npmjs.org. The
# registry is anonymously readable and the trial build does not expire, so `npm ci`
# works for anyone cloning this repository — no account, no token.
@dhx:registry=https://npm.dhtmlx.com
